"use client"

import { useState } from "react"

type PortfolioItem = {
  title: string
  description: string
  image: string
}

const portfolioItems: PortfolioItem[] = [
  {
    title: "استوديو البث المباشر",
    description: "استوديو بث احترافي يعرض واجهات التفاعل والإحصاءات المباشرة للمحتوى.",
    image: "https://i.postimg.cc/cJmbr84W/Whats-App-Image-2026-04-03-at-11-26-40-AM.jpg",
  },
  {
    title: "لوحة أدوات المبدعين",
    description: "واجهة جوال مخصصة لإدارة أدوات المبدعين والإحصاءات.",
    image: "https://i.postimg.cc/MT33Y2BK/Whats-App-Image-2026-04-03-at-11-28-28-AM.jpg",
  },
  {
    title: "واجهة موقع فاخر",
    description: "موقع احترافي مصمم لعرض الهوية والخدمات بأسلوب بصري مميز.",
    image: "https://i.postimg.cc/8P6XLYPx/Whats-App-Image-2026-04-03-at-11-29-24-AM.jpg",
  },
  {
    title: "منصة ألعاب احترافية",
    description: "واجهة مخصصة للبطولات والإحصاءات والتفاعل المباشر.",
    image: "https://i.postimg.cc/525PRFz5/Whats-App-Image-2026-04-03-at-11-30-27-AM.jpg",
  },
  {
    title: "صفحة هبوط مستقبلية",
    description: "تصميم حديث بهوية بصرية فاخرة وواجهة مستقبلية.",
    image: "https://i.postimg.cc/xC8t8xZ2/Gemini-Generated-Image-mwktd4mwktd4mwkt.png",
  },
  {
    title: "استوديو Creator Pro",
    description: "لوحة عرض احترافية للبث المباشر وأدوات التفاعل.",
    image: "https://i.postimg.cc/DfJ3YBwX/Gemini-Generated-Image-8o9elg8o9elg8o9e.png",
  },
]

export function ClientProjects() {
  const [selectedItem, setSelectedItem] = useState<PortfolioItem | null>(null)

  return (
    <section id="client-projects" className="relative py-16 md:py-24">
      <div className="container mx-auto px-4">
        <div className="mx-auto mb-12 max-w-3xl text-center">
          <h2 className="text-3xl font-bold text-foreground md:text-4xl">
            أعمال مختارة
          </h2>
          <p className="mt-4 text-sm leading-7 text-muted-foreground md:text-base">
            نماذج من التصاميم والواجهات التي تم تنفيذها بأسلوب احترافي يعكس جودة العمل
            والهوية البصرية لكل مشروع.
          </p>
        </div>

        <div className="grid grid-cols-1 gap-12">
          {portfolioItems.map((item, index) => (
            <div key={index} className="group">
              <button
                type="button"
                onClick={() => setSelectedItem(item)}
                className="block w-full text-left"
              >
                <div className="overflow-hidden rounded-[32px] bg-black/20 ring-1 ring-white/10 transition-all duration-300 group-hover:ring-primary/40 group-hover:shadow-[0_0_40px_rgba(180,50,50,0.18)]">
                  <img
                    src={item.image}
                    alt={item.title}
                    className="h-[420px] w-full object-cover transition-transform duration-500 group-hover:scale-[1.02] md:h-[560px] lg:h-[720px]"
                    loading="lazy"
                  />
                </div>
              </button>

              <div className="px-1 pt-4">
                <h3 className="text-xl font-bold text-foreground md:text-2xl">
                  {item.title}
                </h3>
                <p className="mt-2 text-sm leading-7 text-muted-foreground md:text-base">
                  {item.description}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>

      {selectedItem && (
        <div
          className="fixed inset-0 z-[9999] flex items-center justify-center bg-black/85 p-4"
          onClick={() => setSelectedItem(null)}
        >
          <div
            className="w-full max-w-6xl"
            onClick={(e) => e.stopPropagation()}
          >
            <div className="overflow-hidden rounded-[32px] bg-[#090909] ring-1 ring-white/10 shadow-2xl">
              <img
                src={selectedItem.image}
                alt={selectedItem.title}
                className="max-h-[82vh] w-full object-contain bg-black"
              />
            </div>

            <div className="mt-4 flex items-start justify-between gap-4">
              <div>
                <h3 className="text-2xl font-bold text-white">
                  {selectedItem.title}
                </h3>
                <p className="mt-2 max-w-3xl text-sm leading-7 text-white/70 md:text-base">
                  {selectedItem.description}
                </p>
              </div>

              <button
                type="button"
                onClick={() => setSelectedItem(null)}
                className="rounded-2xl border border-white/10 bg-white/5 px-4 py-2 text-sm font-medium text-white transition hover:bg-white/10"
              >
                إغلاق
              </button>
            </div>
          </div>
        </div>
      )}
    </section>
  )
}
